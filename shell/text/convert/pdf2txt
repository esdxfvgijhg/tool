#!/bin/sh

for file in */*.pdf; do 
	directory=`dirname "$file"`
	filename=`basename "$file"`
	echo $filename
	extension="${filename##*.}"
	filename="${filename%.*}"
	pdftotext "$file" "$directory/$filename.txt"; 
done


